# Configuration Directory

This directory contains version-controlled experiment configurations.

Configuration files will define dataset settings, temporal representation parameters, model parameters, training settings, and evaluation settings.

The goal is to keep experimental choices explicit and separate from implementation code so that benchmark runs can be reproduced and compared consistently.

No experiment-specific configuration is committed until the corresponding pipeline has been implemented and validated.
